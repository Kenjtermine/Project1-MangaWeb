$kind: http-request
name: Refresh Token
method: POST
url: '{{baseUrl}}/api/auth/refresh-token'
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "refreshToken": "{{refreshToken}}"
    }
order: 3000
